// sneakpack — offline sync via bundle files: pack changes since a cursor,
// carry, apply, verify.
//
// version:    0.1.0
// author:     JaydenCJ
// license:    MIT
// repository: https://github.com/JaydenCJ/sneakpack
// keywords:   offline-sync, sneakernet, airgap, bundle, incremental-backup, directory-sync, integrity
//
// Zero runtime dependencies: the require list below is intentionally empty
// and must stay that way (see CONTRIBUTING.md, "Ground rules").
module github.com/JaydenCJ/sneakpack

go 1.22
